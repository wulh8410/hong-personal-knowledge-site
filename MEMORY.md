# 项目记忆

## 项目基础

- 项目名：hong
- 初始化日期：2026-06-27
- 当前目标：开发第一版个人品牌知识库型官网，V1 不做复杂后台。
- 技术栈：Next.js App Router + TypeScript + Tailwind CSS + Markdown 内容管理 + 静态生成。

## 工作规则

- 每次开始实质工作前先读取 `AGENTS.md` 和 `MEMORY.md`。
- 工作中主动记录架构决策、踩坑、用户纠正、外部资源位置。
- 凭据只记录位置，不记录值。

## 本次记录

- 2026-06-27：当前仓库初始只有个人主页开发方案文档，根目录缺少 `AGENTS.md` 和 `MEMORY.md`；因模板目录 `~/.codex/templates/` 不存在，已按用户提供规则创建本地约束文件。
- 2026-06-27：V1 采用无复杂后台方案，文章放在 `content/articles`，案例放在 `content/cases`，知识库配置集中在 `lib/constants.ts`，内容读取集中在 `lib/content.ts`，便于后续替换为 Decap CMS 等 Markdown CMS。
- 2026-06-27：当前 Windows 环境下 Next.js 16 默认 Turbopack 构建缺少可用原生绑定，`package.json` 已将 `dev` 和 `build` 固定为 `--webpack`，避免本地构建失败。
- 2026-06-27：GitHub 私有仓库已创建并推送到 `https://github.com/wulh8410/hong-personal-knowledge-site`；Vercel CLI 可通过 `npm exec --yes vercel@latest -- ...` 使用，但当前机器没有 Vercel 登录缓存，`vercel whoami` 会等待登录导致超时。
- 2026-06-27：用户反馈已登录 Vercel 后，本进程仍未发现 `~/.vercel` 或 `VERCEL_TOKEN`，`vercel whoami` 与 `vercel deploy --prod --yes` 均超时；下一步需要在当前环境完成 Vercel 登录，或临时提供 `VERCEL_TOKEN` 只用于部署命令。
- 2026-06-27：Vercel 生产部署成功，项目 scope 为 `wulh19841020-9594s-projects`，生产域名为 `https://hong-amber.vercel.app`；部署命令需临时清空非法 `HTTP_PROXY`/`HTTPS_PROXY` 环境变量，例如 `$env:HTTP_PROXY=''; $env:HTTPS_PROXY=''; npm exec --yes vercel@latest -- deploy --prod --yes --scope wulh19841020-9594s-projects`。
- 2026-06-27：首页 V2 重设计方向：采用深色技术感首屏、真实商务照、紧凑能力矩阵、知识库暗色专题区、精选文章、项目经验和微信二维码联系区；移动端隐藏首屏证明卡片以避免过长，保证首屏能看到个人照且无横向溢出。
- 2026-06-28：首页 V3 设计决策：用户明确否定 V2 的深色横幅广告感、零散底部和英文标签；新版改为轻色中文优先首屏，直接说明“帮商家和团队看懂微信生态”，用“我能提供什么”“建议阅读路径”“专题知识库”“联系宏”串起服务价值，底部使用真实微信二维码，二级页可见英文眉标和占位文案同步清理。
- 2026-06-28：ima 技能已安装到本机 `C:\Users\xiang\.codex\skills\ima-skill`，下载源为腾讯 ima 技能包；ima API 凭据需要 `IMA_OPENAPI_CLIENTID` 与 `IMA_OPENAPI_APIKEY` 或 `~/.config/ima/client_id`、`~/.config/ima/api_key`，项目仓库不记录凭据值。当前网站接入思路：ima 作为官方公告、规则、案例的权威资料层和查询层，站内 Markdown 知识库作为宏的解释、路径和方法论层，不直接复制 ima 原始资料到静态页面。
- 2026-06-28：站点已实现 ima 接入 V1.2：新增服务端接口 `/api/ima/search`，前端只展示资料库名称、标题、摘要和媒体类型，不暴露 `kb_id`/`media_id`；首页放轻量官方资料入口，`/knowledge` 和相关专题页提供查询组件。Vercel Production 已配置 `IMA_OPENAPI_CLIENTID` 与 `IMA_OPENAPI_APIKEY` 为 Sensitive 环境变量，凭据值不写入仓库或记忆。
- 2026-06-29：首页微调：首屏个人照改用 `public/images/avatar/hong-stage-background.png` 背景图；联系区不再突出个人微信二维码，改为公众号「宏记」、视频号「吴亮宏」、微信 `wulh8410` 三项文字入口；首页 section 小标签统一为浅绿边框胶囊样式，页脚版权文案居中。
- 2026-06-29：首页信息架构调整：对外措辞将“视频号电商”改为“视频号直播”；专题知识库从 6 个改为 8 个，分别是微信小店、视频号、广告投放、微信推客、微信小店&直播违规规则及解析、AI工具实战、GEO实战探索、微信公私域联运；项目类型改为微信小店培训、知名品牌视频号直播操盘、千万级微信豆代投、微信推客 SaaS、品牌小程序商城、AI 内容与知识库工作流。
- 2026-06-29：首页联系区设计反馈：用户认为右侧白色联系方式卡片像“膏药”；后续深色联系区内的辅助面板应融入暗色体系，避免大面积白底贴片。
- 2026-06-29：已将 `资料/文章集合` 下 58 篇 Markdown 批量导入 `content/articles`；导入规则为保留原文、补齐文章 frontmatter，并按文件夹为主、标题关键词微调归类到微信小店、视频号、广告投放、微信推客、违规规则及解析、微信公私域联运等分类；生成文件记录 `sourcePath` 便于追溯来源。
- 2026-06-30：导入文章排版优化采用渲染层统一处理，不逐篇重写正文；`lib/markdown.ts` 负责清理导出噪音、压缩异常空行、支持原始 HTML 表格并修正飞书导出的 `img href`，`app/globals.css` 统一控制文章正文标题、段落、表格、图片、引用和代码块样式。
- 2026-06-30：文章列表页信息架构调整为“最新 6 篇 + 按分类分区浏览”，分类顺序跟业务结构一致，避免文章数量增加后退化成单一大网格列表。
- 2026-06-30：文章导入清洗补充规则：若正文存在 `<title>...</title>`，优先作为文章标题；删除正文中的 `<title>`、泛化一级标题（如“功能介绍”）和独立分隔线；文章列表首屏文案应面向读者说明可读什么、怎么进入主题，避免“目录/大列表/分类阅读”等内部化表述。
- 2026-07-04：Taste Skill 整套 13 个技能已从 `https://github.com/Leonxlnx/taste-skill` 安装到本机 `C:\Users\xiang\.codex\skills`；需重启 Codex 后加载。
- 2026-07-04：个人 IP 主页大改版参考图采用“实战档案馆”概念，核心是用精密网格表达技术、真实项目照片表达运营、编辑部式信息结构表达知识沉淀；统一使用墨黑/纸白、微信绿、钴蓝和少量朱红，避免紫蓝 AI 渐变、悬浮光球、卡片墙和深色区白色“膏药”面板。12 张独立板块参考图与视觉规范保存在 `design-references/personal-ip-redesign`。
- 2026-07-05：从 2025/2026 微信公开课现场照中选取 4 个差异化场景，制作了 4 张 `16:9` 桌面横版和 4 张 `4:5` 移动端竖版，统一保存在 `design-references/personal-ip-redesign/photo-crops`；后续网页优先使用这些定向构图版本，不直接依赖单一照片的响应式硬裁切。
- 2026-07-05：四张 ima 知识库二维码已按原始字节整理到 `design-references/personal-ip-redesign/knowledge-qr`，保留完整画布、留白、比例和像素清晰度；二维码素材禁止使用生成模型重绘、透视变形或有损压缩，网页仅做等比显示。
- 2026-07-06：个人 IP 官网已按 12 张“实战档案馆”参考图完成首页、文章、知识库、关于我四页的第一轮高保真复刻：统一使用墨黑/纸白、微信绿、钴蓝、朱红与 1440px 网格，页面从卡片墙改为人物档案、编辑部索引、知识码阶梯、项目时间轴和联系信号台；网页素材集中在 `public/images/ip-redesign`。原“透明底图”实际包含烘焙棋盘格，首屏改用有真实活动背景的 `hong-stage-background.png`，关于页使用 `portrait-office.png` 并做边缘融合。1680×945 桌面端已通过内置浏览器核对首页、文章、知识库和关于页，文章主题切换有效；本地未配置 ima 凭据时查询会返回 `skill auth failed`，生产端继续依赖 Vercel 已配置的 Sensitive 环境变量。
- 2026-07-06：移动端静态审计补充约束：知识库装饰图形仅在 `lg` 以上显示，联系信号台在窄屏使用顶部边界、桌面端使用左侧边界，避免 390px 视口出现装饰挤占和分栏压迫；本轮 `npm run lint` 与 `npm run build` 均通过，Next.js 成功生成 81 个页面。内置浏览器仍停留在不可导航的错误页，390×844 最终截图验收需在浏览器会话恢复后补做。
- 2026-07-06：390×844 首页首轮截图显示首屏只露出活动图边缘、人物识别不足；移动端首屏已压缩标题字号、上下间距和人物图区最小高度，目标是在保留主标题与双入口的同时提前约 140px 出现本人照片，桌面断点不变。四张网页知识码与 `design-references/personal-ip-redesign/knowledge-qr` 权威素材的 SHA-256 均完全一致；四个入口页 HTTP 200 且无 Next.js 错误覆盖层标记，`lint` 与 81 页生产构建再次通过。内置 Browser 仍无法创建隔离标签页，最终移动端截图需继续补验。
- 2026-07-06：内置 Browser 在延长等待时间后恢复页面与 CDP 只读校验。390×844 实测发现并修复两处 Grid 最小内容宽度问题：文章主题区原先把整页撑到 `1051px`，增加 `min-w-0` 后整页 `scrollWidth=375`、分类轨道仍保留 `1011px` 内部滚动；知识码轨道原先自身被撑到 `1248px` 并被外层裁掉，修复后轨道视口 `335px`、内容 `1248px` 且可滚动。首页主标题实测为两行，人物图从 `571px` 开始并在首屏露出约 `273px`；关于页人物图从 `644px` 开始且整页无横向溢出。视频号分类切换后首篇更新为“品牌规模化拓客，视频号助力高效增长”，移动菜单 `aria-expanded=true`，控制台无站点错误。Browser 的 `Page.captureScreenshot` 编码仍持续超时，因此最新移动端视觉帧尚未取得；默认视口已恢复。
- 2026-07-06：移动端截图阻塞已连续三轮复现：后台截图、可见模式截图、CDP `Page.captureScreenshot` 与 `Page.startScreencast` 单帧流均在图像编码阶段超时，最后一次还导致 Browser 连接重置；这与页面加载、布局测量和交互无关。Browser 已恢复默认视口并转回后台。高保真目标在最新移动端视觉帧取得前不得标记完成；恢复后直接复查首页、文章、知识库、关于我四页截图，无需重做已通过的尺寸与交互验证。
- 2026-07-06：用户重启后再次恢复目标，Browser 能新建标签页、设置 `390×844` 并加载首页，但截图接口仍立即返回无法捕获；默认视口已恢复。若要在 Browser 修复前完成视觉证据，只能在用户明确允许后使用本地 Playwright 作为截图回退，不能自行绕过 Browser。
- 2026-07-06：用户已明确允许本地 Playwright 作为截图回退。最终生产构建在 `http://127.0.0.1:3001` 启动并完成 `390×844` 四页回归：首页、文章、知识库、关于我均为 HTTP 200，页面 `scrollWidth=390`，无 Next.js 错误覆盖层、图片加载失败或控制台错误；文章分类切换、移动导航和知识码横向滑动均有效。移动端最终约束为：首页标题两行且首屏可识别人物，文章页分类轨道仅内部滚动，知识库页首屏露出知识码索引，关于页标题固定为自然的三行断句。本轮最终 `npm run lint` 与生产构建均通过。
- 2026-07-06：个人 IP 官网改版已提交并推送至 GitHub `main`，功能与素材提交为 `8bb84f1`；Vercel 项目 `hong` 已完成 Production 部署，正式域名继续使用 `https://hong-amber.vercel.app`。部署后首页、文章、知识库、关于我四个入口均返回 HTTP 200，Vercel 状态为 `Ready`；生产部署仍使用既有 Sensitive 环境变量，不在仓库记录凭据值。
- 2026-07-07：二维码展示规则升级：知识库四张知识码从权威海报原图无损裁出 `720×720` 码区，视频号从原始社媒图裁出 `528×528` 码区，网页通过 `unoptimized` 原图显示，禁止 AI 重绘和有损缩放；知识库顶部四项均可点击弹出桌面 500px、移动端 324px 的扫码层。首页“懂技术，也懂运营”现场图桌面端改为顶部对齐，保证“微信公开课 PRO / 微信公开 TALK”完整露出。
- 2026-07-07：ima 查询改为服务端固定映射四个知识库，不再运行时按名称搜索或提供“全部官方资料库”；自然语言整句无结果时按规则、投放、微信豆、推客、违规等领域词降噪重试。四个资料库的真实查询均返回结果，Production 的 `IMA_OPENAPI_CLIENTID` 与 `IMA_OPENAPI_APIKEY` 已更新为 Vercel Sensitive 环境变量，凭据值不进入仓库或记忆。
- 2026-07-07：文章详情页统一为编辑部阅读版式：文章标题区、760px 正文宽度、章节标题、嵌套列表、引用、表格和目录使用同一套渲染规则；Markdown 渲染层会把正文一级标题降为章节标题、清理标题粗体，并在缺少层级时识别独占粗体短句为章节标题，后续导入文章自动继承。
- 2026-07-07：首页首屏个人照从演讲背景图改为 2026 微信公开课现场照，避免右侧主视觉像被裁掉的背景贴图；“懂技术，也懂运营”现场图用 CSS 上移 5px，不改动原始素材。文章详情已支持 `originalUrl` 原文链接字段：有飞书原文 URL 时显示“查看飞书原文”，没有 URL 时只展示 `sourcePath` 导入来源，不伪造不可跳转链接。
- 2026-07-07：知识库搜索从“标题列表”升级为“答案优先 + 来源追溯”：`/api/ima/search` 先返回结构化答复、步骤、清单和命中依据，再返回资料结果；能通过 ima `get_media_info` 拿到 URL 的资料显示“查看来源”，拿不到 URL 的资料提示需在 ima 客户端查看，仍不在前端展示内部知识库或媒体 ID。
- 2026-07-07：当前 `资料/文章集合` 与 `content/articles` 未保存飞书文档级原文 URL，只能找到飞书图片下载地址和部分微信/腾讯官方链接；文章详情的 `originalUrl` 字段已预留，批量补齐需要用户额外提供飞书文档链接清单或重新导出带原文链接的数据。GitHub 仓库当前为 private，已推送 `gh-pages` 跳转分支，但 GitHub API 返回当前计划不支持 private repo 启用 Pages；若必须发布 GitHub Pages，需要用户确认将仓库公开或升级支持 private Pages 的计划。
- 2026-07-07：用户确认公开仓库后，GitHub 仓库 `wulh8410/hong-personal-knowledge-site` 已从 private 改为 public，并启用 GitHub Pages，发布源为 `gh-pages` 分支根目录；Pages 地址为 `https://wulh8410.github.io/hong-personal-knowledge-site/`，当前页面是跳转到 Vercel 正式站 `https://hong-amber.vercel.app/` 的静态入口，因为主站依赖 Vercel Serverless API 承载 ima 搜索。
- 2026-07-07：用户提供 `C:\Users\xiang\WorkBuddy\2026-07-07-17-38-20\微信小店文章整理.md` 作为文章标题到飞书原文的映射来源；已按标题归一化为 51 篇站内文章补齐 `originalUrl`，详情页通过既有“查看飞书原文”按钮跳转。未匹配文章不强行挂链接，避免误导到错误原文。
- 2026-07-07：针对用户反馈，首页首屏人物图从横版现场小人物裁切改为现场背景层叠加高清竖版商务照，`懂技术，也懂运营` 图位调整为上移 1px；关于页左侧身份信息改为深色同体系标签组，项目时间轴加大行高和字号。Ima 搜索继续调用官方 `search_knowledge` 与 `get_media_info`，但前端只在有 `sourceUrl` 时展示“查看来源”，没有网页 URL 的结果不再显示不可点击提示；服务端搜索兜底从泛关键词改为更保守的领域词组合和相关性过滤。
- 2026-07-07：知识库搜索的“商品上架/商品发布/商品管理/场景管理”类问题已增加专门答案模板，直接输出商品上架的资质、基础信息、合规内容、可用场景和审核复查步骤；“命中依据”改为“命中的官方资料”，按资料标题去重，能通过 `get_media_info` 拿到 URL 时显示“查看原文”，不能跳转的资料只展示标题与摘要，避免重复假链接。
- 2026-07-07：首页首屏商务主视觉改为基于真实人物素材重新合成的 `hero-business-portrait.png`，去掉“REAL FIELD PHOTO”等旧卡片文字，采用深色网格、简约建筑光影和微信绿细节，避免证件照式灰墙观感。
- 2026-07-07：知识库搜索答案生成逻辑去掉“先看命中的官方资料”式泛化兜底，新增品牌申请、类目申请等资质/申请类问题模板，并补充品牌、商标、授权、类目准入、报白等检索词；后续搜索结果必须优先给明确处理路径，再展示命中资料。
- 2026-07-07：首页“懂技术，也懂运营”现场图再次微调为 `object-position: center calc(50% - 2px)`，用于遮住顶部露出的少量文案边缘。
